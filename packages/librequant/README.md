# librequant (Python)

Installable package used inside the Jupyter kernel: `from librequant.data import get_bars`.

See the repository root `librequant/README.md` for Docker `PYTHONPATH` and cache location.
