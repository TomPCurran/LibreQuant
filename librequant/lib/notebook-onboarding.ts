/** Notebook filenames referenced in onboarding / library copy — single place to update renames. */
export const ONBOARDING_NOTEBOOKS = [
  "DemoNotebook.ipynb",
  "MlflowExperimentDemo.ipynb",
] as const;
